# Copy this file to config.properties and fill your own values.

# SMTP server settings
smtp.host=localhost
smtp.port=1025
smtp.starttls=false

# Leave empty to disable AUTH LOGIN
smtp.username=
smtp.password=

# Mail defaults
mail.from=
mail.to=

# HTTP fetch default (without protocol prefix)
http.url=example.com/